snippet dfs "dfs" wA
	m, n = len($1), len($1[0])

	def dfs(i, j):
			if i < 0 or i >= m or j < 0 or j >= n or $2:
					return

			$3

			for x, y in [(i - 1, j), (i + 1, j), (i, j - 1), (i, j + 1)]:
				dfs(x, y)

