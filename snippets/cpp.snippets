snippet init "init" wA
	#include <iostream>

	using namespace std;

	#define FOR(i,n) for (int i = 0; i < n; i ++)
	#define SIZE(matrix, m, n) int m = matrix.size(), n = matrix[0].size()
	#define II(v, m, n) vector<vector<int>> v(m, vector<int>(n, 0));

	typedef vector<int> VI;
	typedef pair<int, int> PI;

	void print(vector<int> v) { for (auto &x : v) cout << x << " "; cout << endl; }

	int main() {
		$1
	}
